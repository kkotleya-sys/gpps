interface RouteManagerProps {
  busNumber: string;
  driverId: string;
}

export function RouteManager({ busNumber, driverId }: RouteManagerProps) {
  void busNumber;
  void driverId;
  return null;
}
